#include "Dictionary.h"
#include "Vector.h"
#include "Queue.h"
#include "Stack.h"
#include <iostream>

using namespace std;

struct Point {
    int x;
    int y;
};

std::ostream& operator<< (std::ostream& ostr, const Point& p) {
    ostr << "(" << p.x << ", " << p.y << ")";

    return ostr;
}

int hashPoint(int x, int y) {
    return ((x + y) * (x + y + 1) / 2) + y;
}

int main() {
    Dictionary<int,Point> table;
    // Dictionary<String,int> table2;


    for (int i = 0; i < 95; ++i) {
        table.insert(hashPoint(i, 2*i), {i, 2*i} );
    }


    table.print();

    cout << hashPoint(10000, 9999) << endl;


    return 0;
}
